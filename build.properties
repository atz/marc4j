project.name=marc4j
project.description=MARC Record APIs for Groovy/Java
version=2.5.1-beta
lib.dir=lib
src.dir=src
test.src.dir=test/src
build.dir=build
build.classes.dir=${build.dir}/classes
build.test-classes.dir=${build.dir}/test-classes
build.generated-src.dir=generated-src
dist.dir=dist
apidoc.dir=${build.dir}/apidoc
jar.name=${project.name}-${version}.jar
prefix  =${project.name}-${version}
tmp.dir =${dist.dir}/tmp
tmp.arc.dir=${tmp.dir}/${prefix}
dist.arc=${dist.dir}/${prefix}

